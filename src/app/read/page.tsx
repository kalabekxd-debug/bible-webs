import Link from "next/link";
import { books } from "@/data/bible";
export const metadata = { title: "Firman", description: "Jelajahi kitab dan pasal Alkitab yang tersedia di Sela." };

export default function ReadPage() {
  const groups = ["Perjanjian Lama", "Perjanjian Baru"] as const;
  return <main><section className="border-b border-border"><div className="mx-auto max-w-7xl px-6 py-20 lg:px-10 lg:py-24"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">Scripture</p><h1 className="mt-5 font-serif text-5xl tracking-tight sm:text-7xl">Baca Firman.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted">Jelajahi kitab dan pasal. Sela memisahkan teks Firman dari konten reflektif agar keduanya tetap jelas.</p><div className="mt-8 rounded-2xl border border-border bg-surface px-5 py-4 text-sm leading-6 text-muted">Teks Alkitab yang digunakan Sela akan mengikuti sumber dan ketentuan penggunaan yang berlaku.</div></div></section><section className="mx-auto max-w-7xl px-6 py-16 lg:px-10 lg:py-24">{groups.map((testament) => { const group = books.filter((book) => book.testament === testament); return <div key={testament} className="mb-16 last:mb-0"><div className="flex items-end justify-between gap-6"><h2 className="font-serif text-3xl">{testament}</h2><span className="text-sm text-muted">{group.length} kitab</span></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{group.map((book) => <Link key={book.slug} href={"/read/" + book.slug} className="group rounded-2xl border border-border bg-surface p-6 transition hover:-translate-y-0.5 hover:border-primary"><div className="flex items-center justify-between"><h3 className="font-serif text-2xl">{book.name}</h3><span className="text-sm text-muted group-hover:text-primary">→</span></div><p className="mt-3 text-sm text-muted">{book.chapters} pasal</p></Link>)}</div></div>; })}</section></main>;
}
